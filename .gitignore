# Composer / TYPO3 test instance
/.Build/
/vendor/
composer.lock

# Tooling caches
/.php-cs-fixer.cache
/.phpunit.cache/
/.phpunit.result.cache
/phpstan.neon.local

# TYPO3 runtime artifacts (never part of the extension)
/public/
/var/

# Node (the shipped JS has no build step; the unit tests need no dependencies,
# only the Playwright end-to-end suite does)
node_modules
package-lock.json
/Tests/E2E/test-results/
/Tests/E2E/playwright-report/

# OS / editor
.DS_Store
.idea/
.vscode/
/Tests/E2E/support/.auth/
